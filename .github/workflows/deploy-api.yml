name: Deploy API

on:
  push:
    branches: [main]
    paths:
      - 'services/api/**'
      - '.github/workflows/deploy-api.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy to Azure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # Install at repo root (npm workspaces)
      - name: Install all workspace dependencies
        run: npm ci

      # Build the API workspace
      - name: Build API
        run: npm run build --workspace=services/api

      # Create a standalone deploy package outside workspace context
      - name: Create deployment package
        run: |
          mkdir -p /tmp/deploy
          cp services/api/package.json /tmp/deploy/
          cp -r services/api/dist /tmp/deploy/dist
          cd /tmp/deploy
          npm install --omit=dev --ignore-scripts=false
          ls -la node_modules/better-sqlite3/build/Release/ || echo "No prebuilt binary"
          npm rebuild better-sqlite3
          ls -la node_modules/better-sqlite3/build/Release/
          zip -r $GITHUB_WORKSPACE/deploy.zip package.json dist/ node_modules/

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy pre-built zip to Azure
        run: |
          az webapp deploy \
            --name alerte-neige-api \
            --resource-group alerte-neige-rg \
            --src-path deploy.zip \
            --type zip \
            --timeout 300

      - name: Health check
        run: |
          sleep 30
          for i in 1 2 3 4 5; do
            if curl -sf https://api.alertneige.app/api/health; then
              echo "API is healthy!"
              exit 0
            fi
            echo "Attempt $i failed, waiting 30s..."
            sleep 30
          done
          echo "Health check failed after 5 attempts"
          exit 1
